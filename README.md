# Manual

```bash
composer install
```

preparing the database to work, inserting mock data
```bash
composer prepare
```

running application
```bash
composer start
```

test and sniffer

```bash
composer phpunit
composer sniffer
```
